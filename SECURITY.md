# 数据与安全

`profiles/` 含 Fitbit 健康记录、账户信息、客户端凭据和 OAuth 令牌；`.env` 可能含管理员口令及会话密钥。Git 忽略规则和 Docker 构建规则排除这些运行数据；备份也必须放在仓库外并限制访问。

新安装从 `deploy/.env.production.example` 开始：私有模式、仅本机监听，管理员通过 HTTPS 登录。升级已有部署时保留自己的配置，不要直接覆盖 `.env`。公开源码不代表公开健康数据。应用默认私有；显式公开模式会公开唯一账户的健康数据。即使管理员登录，也无法通过接口访问配置之外的目录。

## 报告问题

向维护者私下报告可复现的问题。若 GitHub 提供私密漏洞报告入口，可通过该入口提交；不要在普通 Issue 发布可用凭据、授权回调 URL 或个人健康数据。使用合成档案提供受影响版本、复现步骤和预期访问边界。

若凭据进入提交历史，应先在凭据提供方撤销或轮换，再清理历史和相关副本。删除最新文件或设为私有仓库，不会使已经暴露的凭据失效。

## 维护边界

`scripts/audit.py` 检查敏感文件路径和私钥标记，它不是完整的凭据检测器。本次迁移还单独检查并清理了继承历史中的一个旧客户端密钥，详见 [来源记录](docs/provenance.md)。不要将未经检查的旧分支或标签重新合并进本仓库。

部署、备份和恢复步骤见 [DEPLOYMENT.md](DEPLOYMENT.md)。
